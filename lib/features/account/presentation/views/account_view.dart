import 'package:flutter/material.dart';

class AccountView extends StatelessWidget {
  const AccountView({super.key});
static const routeName = 'accountview';
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
